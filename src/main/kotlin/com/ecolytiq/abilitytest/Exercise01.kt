package com.ecolytiq.abilitytest

class Exercise01 {

    /**
     * Find all pairs with a given sum in it. If there is no pair return an empty list.
     */
    fun findAllPairs(numbers: List<Int>, sum: Int): List<Pair<Int, Int>> {

        // TODO:
        return emptyList()
    }

    /**
     * Find all unique pairs with a given sum in it. If there is no pair return an empty list.
     */
    fun findAllUniquePairs(numbers: List<Int>, sum: Int): List<Pair<Int, Int>> {

        // TODO:
        return emptyList()
    }
}
